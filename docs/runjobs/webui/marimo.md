The marimo app launches [marimo](https://marimo.io/) on a LUMI compute node, which you can access
from the web interface.

The form for launching marimo provides the same Python environment customization options as the
[Jupyter app](./jupyter.md), with the only difference being that `marimo` must be installed in
Python environments instead of `jupyterlab`.

